Language:php
Database:MySql
ProjectID:md-shazzed-ud-dolla
URL:https://md-shazzed-ud-dolla.uc.r.appspot.com

Use "gcloud init" in command prompt
then select project_id name in this case md-shazzed-ud-dolla
then cd to the folder where the files are to deploy
make sure all the files including the yaml file is there
then use "gcloud app deploy" in command prompt
then use the link to check the website